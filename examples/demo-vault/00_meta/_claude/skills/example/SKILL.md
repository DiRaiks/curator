---
name: example
description: Demo Claude-style skill so the scanner has something to surface under 00_meta/_claude/skills/.
origin: demo
---

# Example skill

Mirrors the real-vault layout: each Claude skill lives in its own directory
containing a `SKILL.md` with `name` and `description` in the frontmatter. The
scanner reports this as `kind: claude-skill`.
